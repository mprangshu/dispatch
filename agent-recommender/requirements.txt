# Runtime + dev dependencies (versions intentionally unpinned for now).
chromadb        # local/persistent vector store: embeddings + metadata
google-genai    # LLM client (configurable; default Gemini per src/config.py MODEL)
pyyaml          # parse agent `.md` frontmatter (lists: tags, autonomy_supported)
python-dotenv   # load API keys / settings from a .env file
fastapi             # HTTP backend (Phase 7) wrapping the chatbot core
uvicorn[standard]   # ASGI server to run the FastAPI app
watchdog        # filesystem watcher for scripts/watch_agents.py (auto re-index)
pytest          # test runner
httpx           # required by FastAPI's TestClient (API tests)
